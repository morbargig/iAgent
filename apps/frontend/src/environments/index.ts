/**
 * Environment Configuration Export
 * 
 * This file exports the environment configuration
 * which is replaced at build time based on the build configuration
 */

export { environment } from './environment';
